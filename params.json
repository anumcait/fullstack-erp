{
  "commands": [
    "cd /opt/erp-app",
    "git pull origin dev",
    "docker compose -f docker-compose.demo.yaml up -d --force-recreate frontend",
    "sleep 8",
    "docker compose -f docker-compose.demo.yaml ps --format '{{.Name}} {{.State}}'"
  ]
}
