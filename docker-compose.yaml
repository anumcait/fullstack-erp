version: '3.9'

services:
  # ────────────── DATABASE (PostgreSQL) ──────────────
  db:
    image: postgres:17
    container_name: hr_postgres
    environment:
      POSTGRES_DB:       hrdb
      POSTGRES_USER:     postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./pg_restore:/pg_backup
      - ./pgdata-new:/var/lib/postgresql/data
      - ./init-scripts/init-db.sh:/docker-entrypoint-initdb.d/01-init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d hrdb && psql -U postgres -d hrdb -c 'SELECT 1'"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 20s

    ports:
      - "5432:5432"
    restart: unless-stopped
    networks: [hr-network]
 
  # db:
  #   image: postgres:17
  #   container_name: hr_postgres
  #   environment:
  #     POSTGRES_DB:       hrdb
  #     POSTGRES_USER:     postgres
  #     POSTGRES_PASSWORD: postgres

  #   volumes:
  #     #- pgdata:/var/lib/postgresql/data
  #     #- ./pg_restore:/pg_backup
  #     - ./pg_restore:/pg_backup
  #     - ./pgdata-new:/var/lib/postgresql/data
  #   #  - /mnt/jenkins-data/postgres:/var/lib/postgresql/data
  #     #- ./pgdata:/var/lib/postgresql/data
  #    # - pgdata:/var/lib/postgresql/data
  #     # optional: mount backup + init script
  #     # - ./init-scripts/01-restore-db.sh:/docker-entrypoint-initdb.d/01-restore-db.sh:ro
  #     # - ./pg_restore/hrdb.backup:/pg_restore/hrdb.backup:ro
  #   healthcheck:
  #     test: ["CMD", "pg_isready", "-U", "postgres"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 10
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped
  #   networks: [hr-network]

  # ────────────── JENKINS (CI/CD) ──────────────
  # jenkins:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.jenkins
  #   container_name: hr-jenkins
  #   user: root
  #   ports:
  #     - "8085:8080"
  #     - "50000:50000"
  #   volumes:
  #     - ./jenkins_data:/var/jenkins_home
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - .:/workspace
  #     # - /d/Fullstack-hr-app:/var/jenkins_home/workspace/fullstack-hr-pipeline
  #   networks: [hr-network]
  #   restart: unless-stopped

  # ────────────── BACKEND ──────────────
  backend:
    build:
      context: ./backend
    container_name: hr-backend
    env_file:
      - ./backend/.env.development
    environment:
      - PYTHON_AGENT_URL=http://agent_python:8001/process
    depends_on:
      db:
        condition: service_healthy
      agent_python:
        condition: service_started
    command: ["npx", "nodemon", "--legacy-watch", "index.js"]
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app           # <-- Mount your source code directory into container
      - /app/node_modules       # <-- Prevent overwriting node_modules inside container
    working_dir: /app            # <-- Set working directory to /app
    networks: [hr-network]
    restart: always

  # ────────────── PYTHON AGENT ──────────────
  agent_python:
    build:
      context: ./agent_python
    container_name: hr-python-agent
    volumes:
      - ./agent_python:/app
    ports:
      - "8001:8001"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]
    networks: [hr-network]
    restart: always

  # ────────────── FRONTEND ──────────────
  frontend:
    build:
      context: ./frontend
    container_name: hr-frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    environment:
      - API_TARGET=http://backend:5000
    ports:
      - "5173:5173"
    networks: [hr-network]
    restart: unless-stopped

  nginx:
    image: nginx:latest
    container_name: hr-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro          # ✅ Mount file correctly
    depends_on:
      - frontend
      - backend
    networks: [hr-network]
    restart: unless-stopped
# ────────────── Networks & Volumes ──────────────
networks:
  hr-network:
    driver: bridge

# volumes:
#   #pgdata:

